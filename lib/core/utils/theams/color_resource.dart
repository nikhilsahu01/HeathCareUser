import 'package:flutter/material.dart';

class ColorResource {
  static const Color primaryBlue = Color(0xFF0165FC);     // Main CTA button, icons
  static const Color lightBlue = Color(0xFFE6F0FF);       // Backgrounds / cards
  static const Color gradientLightBlue = Color(0xFFB9D4FD);       // Backgrounds / cards
  static const Color gradientDarkBlue = Color(0xFF013C96);       // Backgrounds / cards
  // static const Color lightBlue = Color(0xFF1572F6);       // Backgrounds / cards
  static const Color darkText = Color(0xFF1C1C1C);        // Headlines
  static const Color pillBlue = Color(0xFFB7C3F3);        // Headlines
  static const Color lightText = Color(0xFF6B6B6B);       // Subtext
  static const Color background = Color(0xFFF9F9F9);       // Scaffold bg
  static const Color lightRed = Color(0xFFD9534F);       // Scaffold bg
  static const Color lightPink = Color(0xffFECCE2);       // Scaffold bg
  static const Color white = Colors.white;
  static const Color green = Color(0xFF95CD41);           // Banner background
  static const Color lightGreenBackground = Color(0xFFD9FFD0);          // Banner background
  static const Color red = Color(0xFFFF3B30);             // Emergency icon/button
  static const Color shadow = Color(0x14000000);          // Card drop shadows
  static const Color greyBorder = Color(0xFFE0E0E0);      // Divider or card border
}
